\chapter{Licences}\label{cha:licences}\markboth{Licences}{Licences}

The Thank You chapter (page~\pageref{cha:thanks}) is the thanks; this
is the record. The
authoritative copy is \verb|LICENSES.md| in the repository --- if the
two ever disagree, that file wins.

\section{The project itself}
The BMC-K4510 as a whole is distributed under the \textbf{GNU General
Public License, version~2 or (at your option) any later version}.
Everything written for this project --- the machine, VICKe, SHEILA,
the DMA engine, the MATH unit, the system ROM and K/OS, JIM, the Tube
ULA, the network layer, the editors, the demos, the Pascal target and
the Raspberry~Pi port --- is Copyright \copyright{} 2026 Michael
Longval and is released under those terms. The choice is not really a
choice: the CPU core and reSID are GPL-2.0-or-later, and a work
containing them must be too.

\section{Code inside the machine}
\begin{itemize}[leftmargin=1.4em]
\item \textbf{Xemu 65xx/45GS02 CPU core} --- Gábor Lénárt.
  \verb|core/xemu/|: \verb|cpu65.c|, \verb|cpu65.h|,
  \verb|cpu65_mega65_timings.h|, \verb|cpu65ce02_disasm_tables.c|,
  used unchanged. \emph{GPL-2.0-or-later.}
\item \textbf{reSID} --- Dag Lem, as shipped in VICE~3.3.
  \verb|core/resid/|. \emph{GPL-2.0-or-later.}
\item \textbf{BBC BASIC, console edition (BBCTTY)} --- Richard T.
  Russell. \verb|tube/|, vendored and altered (see
  \verb|tube/ALTERED.md|). \emph{zlib licence.} The name ``BBC BASIC''
  is used by permission; that permission is personal to this project
  and is \emph{not} transferable to forks or derivatives.
\item \textbf{EhBASIC 2.22} --- Lee Davison, in the ca65 form from
  jefftranter/6502. \verb|basic/basic.asm|. \emph{Free for
  non-commercial use only}; derivatives must carry the words
  ``Derived from EhBASIC'' --- see \verb|basic/README-EhBASIC.txt|.
  It ships as a separate program (\verb|fs/ehbasic.prg|) and is not
  linked with the GPL code.
\item \textbf{RunCPM} --- Marcelo Dantas. \verb|cpm/src/|, vendored
  unmodified (\verb|cpm/VENDORED-FROM.txt|). \emph{MIT.}
\item \textbf{Tali Forth 2} --- Scot W. Stevenson, Sam Colwell,
  Patrick Surry. \verb|forth/tali/|, vendored unmodified.
  \emph{Public domain.}
\item \textbf{Wozmon} --- Steve Wozniak's 1976 monitor, reimplemented
  here for the 45GS10. Apple published the original listing; this
  machine's version is the project's own code under the project
  licence.
\item \textbf{cc65 runtime} --- the ROM and every \verb|.prg| are
  linked against \verb|none.lib|. \emph{cc65's zlib-style licence.}
\item \textbf{Microsoft 6502 BASIC 1.1} (planned, via mist64/msbasic)
  --- \emph{MIT}, released by Microsoft in 2025. Not yet in the
  machine.
\end{itemize}

\section{Fonts}
\begin{itemize}[leftmargin=1.4em]
\item \textbf{Linux kernel 8x8 console font} --- \verb|data/font8.bin|,
  built by \verb|data/mkfont.py| from \verb|lib/fonts/font_8x8.c|.
  \emph{GPL-2.0.} This is the default text font.
\item \textbf{MEGA65 open-roms chargen + PXLfont 2.3} ---
  \verb|data/fonts/openroms/|. Clean-room C64/C65 character shapes;
  PXLfont is Retrofan's, included with open-roms' recorded permission.
  \emph{LGPL-3.0-or-later} --- \verb|8x8font.png| is shipped as the
  editable source, for LGPL compliance.
\item \textbf{unscii-8} --- Ville-Matias Heikkilä.
  \verb|data/fonts/unscii/|; \verb|font8-unscii.bin| is a generated
  drop-in alternative to \verb|data/font8.bin|. \emph{Public domain.}
\item \textbf{BESCII v3} (Mono + source) --- Damian Vila.
  \verb|data/fonts/bescii/|. \emph{CC0-1.0.}
\item \textbf{Clear Sans} (Intel, \emph{Apache-2.0}) and
  \textbf{Iosevka} (Belleve Invis, \emph{SIL OFL 1.1}) --- the two
  faces this book is set in. They are not part of the machine.
\end{itemize}

\begin{aside}
\textbf{No Commodore ROMs here.} Until 2026-08-23 the text font was
derived from a Commodore~64 character ROM. That file and every
derivative were removed from the repository \emph{and from its
history} before it was made public. If you own a C64 and want the real
thing, drop your own \verb|chargen.bin| into \verb|/SYSTEM|: the
machine will use it, and \verb|.gitignore| will keep it out of the
repository. That copy is yours, not ours to publish.
\end{aside}

\section{The Raspberry Pi build}
Neither of these lives in the repository; \verb|pi/Makefile| expects
them beside the checkout.
\begin{itemize}[leftmargin=1.4em]
\item \textbf{Circle} --- Rene Stange. \emph{GPL-3.0.}
\item \textbf{circle-libsdl2} --- Xalior. \emph{zlib}; its
  \verb|sdl-app.ld| is \emph{GPL-3.0}.
\end{itemize}
A \verb|kernel8.img| built from these is therefore GPL-3.0 as a whole,
which GPL-2.0-\emph{or-later} code permits. The Raspberry~Pi boot
firmware in a distributed card image (\verb|bootcode.bin|,
\verb|start.elf|, \verb|fixup.dat|) is Broadcom's, redistributable
with Raspberry~Pi hardware under its own licence.

\section{Build tools}
Not distributed with the machine, but required to build it: GCC and
GNU Make (\emph{GPL}), cc65 (\emph{zlib-style}), 64tass
(\emph{GPL-2.0}), NASM (\emph{BSD-2-Clause}), SDL2 (\emph{zlib}),
Python~3 (\emph{PSF}), and for this book XeLaTeX (\emph{LPPL}). Mad
Pascal and MADS (Tomasz Biela, \emph{MIT}) are needed only if you
compile Pascal; the K4510 target in \verb|pascal/| is installed into
your own Mad Pascal checkout.

\section{What is deliberately not shipped}
\begin{itemize}[leftmargin=1.4em]
\item \textbf{The CP/M system disk.} RunCPM's \verb|DISK/A0.zip| ---
  Digital Research's \verb|ASM|, \verb|MAC|, \verb|DDT|, \verb|STAT|,
  \verb|SUBMIT| and third-party tools --- installs into
  \verb|fs/CPM/A/0/| for your use but is not committed: its files have
  mixed provenance and this repository is public. The same goes for
  WordStar, Turbo Pascal~3 and MBASIC, which the \verb|K-*.SUB|
  launchers know how to start but which you must supply yourself.
\item \textbf{SID tunes.} \verb|fs/SID| is a symbolic link to a local
  copy of the High Voltage SID Collection. The tunes are the
  composers' copyright, are used here only for listening, and are not
  part of this repository or of any release.
\item \textbf{A Commodore character ROM} --- see the note above.
\item \textbf{Your \texttt{STARTUP.BAT}} --- yours, not the repository's
  (copy \verb|/SYSTEM/STARTUP.SAMPLE| to start one).
\end{itemize}

\section{If you redistribute the machine}
Ship the sources you built from, keep \verb|LICENSES.md| and this
chapter with them, keep \verb|8x8font.png| beside the open-roms font,
and remember two things that are easy to forget: EhBASIC is
non-commercial-only, and the ``BBC BASIC'' name is licensed to this
project and not to yours. Call your fork's BASIC something else.

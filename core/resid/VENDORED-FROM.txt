commit not determined -- vendored 2026-08-22 (repo commit 32feb45)
Dag Lem's reSID, taken as shipped in VICE 3.3
(https://sourceforge.net/projects/vice-emu/), used unchanged.

All four SID chips run on this.  GPL-2.0-or-later.

What is known.  The release it came from is VICE 3.3, which is a firmer
statement than a commit would be for a library distributed inside
another project.  "version 2" in the headers is the SID model constant,
not a release number -- do not mistake it for one.  reSID itself has no
version string in these files.

To settle it:
  # fetch the VICE 3.3 tarball and compare the directory
  diff -r vice-3.3/src/resid <this repo>/core/resid

Content digest of the .cc/.h here (sorted, concatenated): 41328e368449fb65

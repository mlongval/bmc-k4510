# Handbook session — status

Protocol: `docs/AGENTS.md`. This file is the handbook session's; the
coding session does not write to it.

(Started by the coding session so the file exists — overwrite freely.)
